# Session log — 20. 6. 2026 (audit)

> Pracovní session: 14:30 — 16:10 GMT+2 (~1h 40min práce)
> 5 commitů, všechny v main, working tree čistý.

---

## 📊 Výsledky session

| # | Task | Commit | Effort | Výsledek |
|---|------|--------|--------|----------|
| 1 | P0 broken obrázky (4 rapperi) | `3588ee9` | 30 min | Hasan, Redzed, Strapo, Tafrob — Deezer 1200×1200 WebP |
| 2 | Schema.org JSON-LD | `9fea140` | 50 min | 12 typů entit, 542 řádků |
| 3 | Top 21-50 shortTag | `6afb40c` | 30 min | 28 nových + 2 skeleton profily |
| 4 | Sitemap.xml + robots | `d50f7ee` | 25 min | 1101 URL, xmllint valid |
| 5 | Style→genre validation fix | `6e4cbea` | 25 min | -95% INVALID_REL errors (323→16) |

**Celkem**: 5 commitů, ~140 minut, +2400 řádků (kód + data)

---

## 🔧 Klíčová technická rozhodnutí

### Image fix (commit 3588ee9)
- **HEARTBEAT.md lhal**: Hasan měl malý 900×900 base44 WebP, Tafrob měl stejný SVG-fallback bug jako redzed/strapo. Všichni 4 sjednoceni na 1200×1200 z Deezeru.
- **Source**: `deezer-index.json` v `.content-cache/` měl `exact` match pro všechny 4 (již z dřívějška).
- **Pipeline**: `curl → 500x500 JPEG → PIL resize 1200x1200 WebP q82` (LANCZOS).
- **Bonus**: aktualizoval jsem `profile.json` (profileImageUrl, photoSourceUrl, photoConfidence: 90) + odebral dangling `image` field u tafrob/meta.json.
- **Single source of truth pro artist obrázky**: `src/lib/content/images.ts` (`getArtistImage()`). `profile.json.profileImageUrl` se ukládá do cache, ale stránka ho nečte.

### Schema.org (commit 9fea140)
- **Nový soubor**: `src/lib/seo/schema-org.ts` (16 kB) — pure funkce `buildJsonLd()` per type
- **Nový soubor**: `src/components/seo/JsonLd.tsx` (1 kB) — SSR `<script type="application/ld+json">` wrapper
- **12 typů entit**: artist→MusicGroup, album→MusicAlbum, label→MusicRecordLabel, collective→MusicGroup, producer→Person, location→City, scene→Place, genre/style→Genre, article→Article, theme/mood/track/event→Thing
- **Relations v JSON-LD**: `genre`/`foundingLocation`/`member`/`recordLabel` všechny přes `@id` URL (ne textové labely)
- **Draft handling**: `isIndexable()` vrací `false` pro status=draft → negeneruje markup
- **OG metadata rozšíření**: `og:image` (artist z images.ts, album z cover), `og:type=music.song`, `twitter:card=summary_large_image`, `canonical` URL
- **TS type fix**: `CacheEntity.image: string|null`, normalizace v cache-builder

### Top 21-50 shortTag (commit 6afb40c)
- **Vstup**: `memory/rapper-tags-proposal.md` — Peter měl připravených 30 jmen s A/B/C variantami
- **Skript**: `scripts/add-short-tags-21-50.ts` — idempotentní, dry-run, force
- **Rozhodnutí**: vždy varianta A (Peterova preference). B-čka nechám na případný `--force` override
- **Skeleton profily**: Koky + Grey256 (entity složky existovaly, profile.json ne). Vytvořeny s krátkým popisem + shortTag + sources. Plný profil doplní Peter.
- **Všechny tagy < 120 znaků** (schema limit)
- **Selection logic**: 28 z inbound rank (proxy popularity na 4rap.cz)

### Sitemap (commit d50f7ee)
- **Nový soubor**: `src/app/sitemap.ts` — Next.js MetadataRoute.Sitemap pattern
- **Nový soubor**: `src/app/robots.ts` — MetadataRoute.Robots
- **1101 URL**: 15 static (homepage/mapa/listing pages) + 1086 entity (drafty vynechány)
- **Tier priority**: artist/album/track=0.9, label/collective/scene=0.8, genre/style/theme/mood/location/producer=0.7, article/event=0.6, listing=0.5
- **lastmod**: `entity.updatedAt || entity.publishedAt || today`
- **Validace**: xmllint OK, python ElementTree OK, 0× draft v sitemap ✅

### Style→Genre fix (commit 6e4cbea)
- **Problém**: `relations.json` měl `styles: ["drill", "grime", ...]` což se resolvuje na `genre_*`, ale registr `HAS_STYLE → [style]`
- **Skript**: `scripts/migrate-relations-styles-to-genres.ts`
  - `genre_*` reference v styles → přesun do dedupovaného `genres` pole
  - dangling → odebrat
  - wrong-type (scene/mood/theme/album) → odebrat + warning
- **Idempotentní**: re-run = 0 changes
- **103 souborů modifikováno**, +592/-826 řádků

---

## 📈 Stav projektu po session

| Metrika | Ráno | Teď | Změna |
|---------|------|-----|-------|
| Entities | 1349 | 1349 | 0 |
| Edges | 5684 | 5439 | -245 (migrace) |
| Backlinks | 1759 | 1638 | -121 (migrace) |
| Backlinked entities | 1071 | (nezměněno) | |
| Typy entit | 12 | 12 | |
| Draft (noindex) | 153 | 153 | |
| Validation errors | 363 | 56 | **-85%** |
| INVALID_RELATION_TARGET_TYPE | 323 | 16 | **-95%** |
| Working tree | clean | clean | |
| Top 50 shortTag | 20 | **50** | **+150%** |
| Build status | SKIP_VALIDATION needed | SKIP_VALIDATION still needed | (16 errors zbylých) |

---

## 🚧 Známé issue (mimo scope, TODO)

### Blokuje build (16 errors)
- 14× `artist.themes references location_*` — autoři chtěli "město v themes", správně by mělo být v `locations`
- 2× `artist.themes references scene_*` — dtto

### Dangling relations (486 total, 410 unique slugs)
- Top: `non-ivot` (28×), `vztahy` (25×), `rb` (12×), `penze` (10×), `spch` (9×)
- Řešení: buď vytvořit chybějící theme/mood/style entity, nebo vyhodit z relations

### EMPTY MDX u locations (40)
- Většina měst (Brno, Praha, Česko, Jihlava, …)
- Description je v meta.json, MDX je volitelný
- Lze doplnit copy-paste z description

### Minor warnings (non-blocking)
- 7× MISSING_PUBLISHED_AT (58g, radikal-chef, francie-jizni, …)
- 4× EMPTY_DESCRIPTION (francie-jizni, presov, tabor-jizni-cechy, …)

### Image quality queue (z HEARTBEAT P1)
- 12× artist obrázků 900×900 (ne standard 1200×1200)
- 276× artistů bez obrázku (scope/draft, edge-only)
- 126× alb bez `tracks.json`

---

## 🎯 Plán na další session

### Rychlé wins (~1h)
1. **15 min**: Manuální cleanup 16 `themes → location/scene` errors (každý specifický)
2. **15 min**: Doplnit EMPTY MDX u 40 lokalit (krátké texty z description)
3. **30 min**: Test Schema.org Markup v Google Search Console (rich results test)

### Střední wins (~2h)
4. **1 hod**: Entity creator skript — top 50 dangling slugs → nové theme/mood/style entity
5. **1 hod**: Image batch upgrade — 12× 900×900 → 1200×1200 z Deezer

### Velký scope (~3-4h)
6. **3-4 hod**: Plná databáze schema/registry refactor — internalizovat nové edge types (HAS_GENRE_ALBUM, COLLABORATED_WITH), rozšířit HAS_THEME o location jako sub-type, atd.

---

## 📝 Důležité poznámky

### Scope adherence
- Plán odhadoval 5-6h práce na dnešek. Skutečnost: 1h 40min (začátek v 14:30 místo 9:00). Vše podle plánu.
- Scope creepu jsem se vyhnul: `themes → location` (14) jsem nechal na další session, místo toho jsem commitoval s dokumentací scope.

### Trade-offs
- **Schema.org**: varianta "generovat univerzálně pro všechny typy" vyhrála nad "generovat jen pro Tier 1" — kód je jednodušší, drafty se filtrují samy.
- **Sitemap**: varianta "generovat dynamicky z cache" vyhrála nad "statický build-time soubor" — automaticky reaguje na změny v datech.
- **shortTag**: varianta "vždy A" vyhrála nad "inteligentní výběr z A/B/C" — Peter je kurátor, ne AI.

### Lesson learned
- HEARTBEAT.md občas lže (Hasan/Tafrob nebyli OK, jak tvrdil). Vždy ověřit realitu dat před akcí.
- Validátor správně hlásil chyby, ale jeho zpráva `target "${resolved}" has invalid type "${targetEntity.type}"` je méně informativní než formát z registru (`expects type in [style], got "genre"`). Na diagnostiku je potřeba vlastní helper.

---

## 🔗 Reference

- HEARTBEAT.md (status snapshot)
- rapper-tags-proposal.md (zdroj pro shortTag)
- dnesni_rozvrh.md (ranní plán)
- git log --oneline | head -10 (historie commitů)
